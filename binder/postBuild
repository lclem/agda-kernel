#!/bin/bash

make local-install
make kernel-install
make codemirror-install

apt-get install agda-mode
apt-get install agda-stdlib

#pip install agda_kernel

#cabal update
#cabal install Agda
#agda-mode setup
